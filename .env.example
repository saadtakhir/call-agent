# --- Auth ---
# Always-available fallback admin account (full access) — additional
# logins with their own permissions are created from the app's
# Foydalanuvchilar page once this one is set up.
ADMIN_USERNAME=
ADMIN_PASSWORD=
# Any long random string — used to sign the session cookie. Generate with:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=

# --- Database (Neon Postgres recommended, but any Postgres works) ---
DATABASE_URL=
# Neon's unpooled/direct connection string — used by Prisma CLI (migrate/db push).
# If you don't have a separate unpooled URL, just repeat DATABASE_URL here.
DATABASE_URL_UNPOOLED=

# --- Vercel Blob (audio caching: greeting, canned responses, per-property replies) ---
BLOB_READ_WRITE_TOKEN=

# --- OpenAI (agent brain + default STT provider) ---
OPENAI_API_KEY=
# OPENAI_TRANSCRIBE_MODEL=gpt-4o-transcribe
# OPENAI_CALL_MODEL=gpt-5.6-terra
# Separate organization-level Admin API key (api.usage.read scope) — only
# needed to show OpenAI cost data on the Xarajatlar panel; OPENAI_API_KEY
# above has no permission to read usage/cost endpoints. Create one at
# platform.openai.com under Organization -> Admin keys.
# OPENAI_ADMIN_API_KEY=

# --- ElevenLabs (TTS, and an alternative STT provider) ---
ELEVENLABS_API_KEY=
ELEVENLABS_VOICE_ID=
# ELEVENLABS_CALL_MODEL_ID=eleven_v3_conversational

# --- Muxlisa AI (Uzbek-specialized alternative STT provider) ---
# MUXLISA_API_KEY=

# --- uy-joy.uz property catalog (defaults already point at the live site) ---
# UY_JOY_API_BASE=https://api.uy-joy.uz/api/public/product
# UY_JOY_CATALOG_URL=https://api.uy-joy.uz/api/public/main/catalog/search

# --- Telegram (optional — captured leads are forwarded here if set) ---
# TELEGRAM_BOT_TOKEN=

# --- Supabase Realtime (optional — instant "Tugatish" push to an active
# call, see lib/callHangupRealtime.js; without this, hangup still works via
# the existing 30s polling fallback, just slower) ---
# Project URL + "anon public" key from Settings -> API on a Supabase
# project (a free project used ONLY for this broadcast channel — no
# database/auth features of Supabase itself are used). NEXT_PUBLIC_-
# prefixed because components/AiCallWidget.jsx (a browser component) needs
# these directly — the anon key is meant to be public, same as any other
# client-side app config.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
